# analog-pd-dashboard

This repository provides a simple dashboard for semiconductor competitor data.

Run locally:

    python -m venv .venv
    source .venv/bin/activate
    pip install -r requirements_cloudrun.txt
    python app.py

